---
const { eventData } = Astro.props;
---

<div class="eventcard-container">
  <div class="img-container">
    <img src={eventData.img} alt="" />
    <div>
      <p>{eventData.dato}</p>
    </div>
  </div>
  <div class="text-container">
    <p class="text-bold">{eventData.event}</p>
    <p>{eventData.beskrivelse}</p>
  </div>
</div>

<style>
  .eventcard-container {
    margin: 2rem 4rem;
    background-color: #bcceff;
  }

  /* img container */
  .img-container {
    display: grid;
  }

  .img-container img {
    grid-area: 1/1;
    object-fit: cover;
    width: 100%;
    height: 100%;
  }

  .img-container div {
    grid-area: 1/1;
    background-color: #fff;
    width: 120px;
    height: 80px;
    left: 30px;
    position: relative;
  }

  .img-container p {
    place-self: center;
    font-weight: 400;
    margin-top: 30px;
    font-size: 18px;
  }

  /* text container */
  .text-container {
    margin: 1rem;
    place-self: center;
  }
  .text-bold {
    font-weight: 600;
    margin-bottom: 2ch;
  }

  /* desktop view */
  @media (width>=800px) {
    .eventcard-container {
      display: grid;
      grid-template-columns: 2fr 1fr;
    }
  }

  @media (width<800px) {
    .eventcard-container {
      padding-bottom: 2rem;
    }
  }
</style>
